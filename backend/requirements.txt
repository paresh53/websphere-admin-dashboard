# ── Core (always required) ──────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0
pyyaml==6.0.1
python-dotenv==1.0.1

# ── Optional: SSH support (only when simulation_mode: false) ────────
# Install manually if you need real server connectivity:
#   pip install paramiko==3.5.0
paramiko==3.5.0
