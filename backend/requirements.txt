# ── Core (always required) ──────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.29.0
pyyaml==6.0.1
python-dotenv==1.0.1

# ── SSH support (for WAS/ODR/CPE/ICN management) ────────────────────
paramiko==3.5.0

# ── WinRM support (for IIS management) ──────────────────────────────
pywinrm==0.4.3

# ── Production server (replaces uvicorn for deployment) ────────────
gunicorn==22.0.0

# ── Logging and monitoring ──────────────────────────────────────────
python-json-logger==2.0.7

# ── Packaging (only needed for building EXE) ───────────────────────
# PyInstaller==6.1.0  # Install separately if building EXE

